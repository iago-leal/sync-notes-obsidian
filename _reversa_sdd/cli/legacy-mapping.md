# Legacy Mapping — unit `cli` (stub)

| Arquivo | Linhas | Papel |
|---|---|---|
| `src/synctotes/cli.py` | 1-23 | `main() -> int`: imprime versão + aviso, retorna 0 (entry point verificado end-to-end) |
| `pyproject.toml` | 25-26 | Console script `synctotes = "synctotes.cli:main"` |

🔴 LACUNA: comandos reais da CLI (Typer) não especificados no código; plano em F6 do ciclo MDCU.
